<h1>All Tracks</h1>
<% @tracks.each do |track| %>
  <div class="track">
    <h3><%= track.song_title %> <small>(by: <%= track.author %>)</small></h3>
    <p>
      <%= track.content %>
    </p>
  </div>
<% end %>